// TODO(Phase 4): replace this stub with the real forgot-password screen.
import { SafeAreaView } from "react-native-safe-area-context";
import Text from "@/components/Text";

export default function ForgotPasswordStub() {
  return (
    <SafeAreaView className="flex-1 bg-bg-base items-center justify-center">
      <Text>TODO: forgot-password</Text>
    </SafeAreaView>
  );
}
