// TODO(Phase 4): replace this stub with the real session-expired screen.
import { SafeAreaView } from "react-native-safe-area-context";
import Text from "@/components/Text";

export default function SessionExpiredStub() {
  return (
    <SafeAreaView className="flex-1 bg-bg-base items-center justify-center">
      <Text>TODO: session-expired</Text>
    </SafeAreaView>
  );
}
