// TODO(Phase 4): replace this stub with the real reset-password screen.
import { SafeAreaView } from "react-native-safe-area-context";
import Text from "@/components/Text";

export default function ResetPasswordStub() {
  return (
    <SafeAreaView className="flex-1 bg-bg-base items-center justify-center">
      <Text>TODO: reset-password</Text>
    </SafeAreaView>
  );
}
